const config = {

    port : 3000,

    storage : './service_storage',

    mime : 'text/plain', // MIME sending, from client to api
                         // api allows : text/*
};

//

module.exports = config;