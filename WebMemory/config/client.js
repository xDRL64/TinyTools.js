const config = {

    ip   : '127.0.0.1', // use the exact same in client web browser
                        // 'localhost' !== '127.0.0.1'

    port : 5500,

};

//

module.exports = config;